Act as a Principal Engineer, Prompt Engineer, and GitHub Copilot Agents Expert.

Context:
I am building an "Agent-Driven Development System" that automates the full SDLC lifecycle using Agents and Agent Orchestration. The system takes inputs like PRD, BRD, Features, and Requirements, and executes end-to-end software development.

Your Tasks:

1. COMPREHENSIVE REVIEW
- Review the current architecture, workflows, and agent orchestration.
- Validate alignment with the latest GitHub Copilot Agents capabilities, patterns, and best practices.
- Identify gaps, inefficiencies, or anti-patterns in:
  - Agent orchestration
  - Task decomposition
  - Context handling
  - Execution flow

2. AGENT ARCHITECTURE VALIDATION
- Verify if the system correctly uses:
  - Multi-agent architecture (Planner, Builder, Tester, Reviewer, Deployer, etc.)
  - Sub-agents (or equivalent modular agents) to optimize context window usage
- If not:
  - Refactor into hierarchical agents (Main Agent → Sub Agents)
  - Ensure each sub-agent has a well-defined scope and minimal context

3. CONTEXT WINDOW OPTIMIZATION
- Analyze how context is passed across stages
- Introduce:
  - Context summarization between stages
  - Memory artifacts (structured outputs like JSON, markdown contracts, task outputs)
  - Stateless execution where possible
- Prevent context bloat and redundant information passing

4. SDLC PIPELINE VALIDATION
Ensure the pipeline enforces strict stage gates:

Stages:
- Requirement Analysis (PRD/BRD parsing)
- Feature Breakdown
- Design (HLD/LLD)
- Implementation
- Testing (unit/integration)
- Review (code quality, standards)
- Deployment readiness

For each stage:
- Define clear input/output contracts
- Add validation checks before moving to next stage

5. VALIDATION & QUALITY GATES
- Implement validation between every stage:
  - Requirement completeness
  - Design consistency
  - Code correctness
  - Test coverage
- Add automated checks:
  - Linting
  - Static analysis
  - Schema validation
  - Contract validation between agents

6. ITERATIVE FIX LOOP (SELF-HEALING SYSTEM)
- Introduce a loop mechanism:
  IF (tests fail OR validation fails OR requirements missing):
    → Identify root cause
    → Route back to appropriate agent (not always the same stage)
    → Fix and re-run pipeline
- Ensure loop continues until:
  - All tests pass
  - Requirements are fully implemented
  - No validation errors remain

7. EXECUTION & VERIFICATION
- Ensure system:
  - Runs the application after build
  - Executes all tests
  - Verifies runtime behavior
- Add:
  - Smoke tests
  - Health checks
  - Basic runtime validation

8. OUTPUT ARTIFACTS
- Generate structured outputs at each stage:
  - Parsed requirements
  - Feature list
  - Design documents
  - Code modules
  - Test reports
- Maintain traceability from PRD → Code → Tests

9. IMPROVEMENTS & REFACTORING
- Suggest improvements for:
  - Scalability
  - Parallel execution of agents
  - Fault tolerance
  - Observability (logs, traces per agent)
- Introduce retry strategies and fallback mechanisms

10. FINAL DELIVERABLE
Provide:
- Improved architecture diagram (textual or structured)
- Refactored agent workflow
- Example agent definitions (roles + responsibilities)
- Pseudocode or actual implementation for orchestration loop
- Sample contracts between stages
- Best practices aligned with GitHub Copilot Agents

Constraints:
- Keep system modular and extensible
- Prefer clarity over complexity
- Optimize for real-world enterprise usage
- Avoid unnecessary verbosity in agent communication

Goal:
Transform this into a robust, self-healing, agent-driven SDLC system aligned with modern GitHub Copilot Agents patterns.



## Plan: Agent-Driven SDLC System — Comprehensive Overhaul

**TL;DR:** The existing 10-phase pipeline is architecturally solid but has 12 key gaps. Merge `full-lifecycle` + `orchestrator` into a single `pipeline-orchestrator` agent, then address context window bloat, missing per-phase micro-gates, narrow self-healing scope, absent runtime verification, weak observability, and no budget guards — across 6 implementation phases.

---

### Findings: Current Gaps (Critical → Nice-to-Have)

| # | Gap | Impact | Location |
|---|-----|--------|----------|
| 1 | `full-lifecycle.agent.md` + `orchestrator.agent.md` duplicate ~400 lines (same 10-phase table, same hard rules, same workspace analysis) | Drift risk, confusing picker | Both files |
| 2 | Context passed as raw YAML blobs — Phase 3 receives full manifest + full blueprint inline | Context window overflow on large PRDs | orchestrator Phases 2–10 prompts |
| 3 | Self-healing loop only triggers from Phase 7 — compile failures at Phase 5a reach Phase 6 unchanged | Wasted work, false failures | orchestrator auto-fix section |
| 4 | No per-phase micro-gates — a broken OpenAPI spec isn't caught until code generation fails | Late failure discovery | Phase 4→5 boundary |
| 5 | All agents have `agents: ['*']` — no routing discipline; any agent can delegate to any other | Circular delegation, unintended cascades | All 53 agent frontmatters |
| 6 | No application runtime verification — mvn test passes but app won't start | Smoke tests not verified at runtime | Phase 7 |
| 7 | No structured observability — no trace IDs, no per-phase structured logs, no pipeline-trace.json | Can't diagnose silent failures | pipeline-wide |
| 8 | No budget guards — no cap on sub-agent delegations or token consumption | Runaway cost / infinite cascade | orchestrator |
| 9 | "Parallel Phase 5" is aspirational — VS Code Copilot executes agent() calls sequentially | Misleading documentation | orchestrator Phase 5 |
| 10 | HLD → Code directly; no LLD phase for HIGH/CRITICAL complexity | Underspecified component design | Phase 3→4 boundary |
| 11 | Partial re-validation not implemented — auto-fix always re-runs complete Phase 7 | Wasted validation effort | auto-fix loop |
| 12 | No `traceability-matrix.json` built incrementally — only assembled in Phase 10 | Late traceability, no incremental check | Phases 1–9 |

---

### Phase A: Consolidation *(Step 1–3)*

**Step 1.** Merge `full-lifecycle.agent.md` + `orchestrator.agent.md` → **`.github/agents/pipeline-orchestrator.agent.md`**
- Absorb full-lifecycle's workspace classification logic as Phase 0 pre-flight (it already functioned as Phase 0 — just unlabeled)
- Eliminate duplicated 10-phase table, hard rules, workspace type matrix
- Single entry in VS Code agent picker
- Explicit `agents: [requirement-analyst, product-decomposition, architect, api-designer, openapi, asyncapi, backend-java, spring-boot, frontend-react, database-engineer, ai-ml-engineer, testing-qa, validation-qa, documentation-writer, devops-engineer, stage-gate]` — no `'*'` wildcard

**Step 2.** Archive originals: rename `full-lifecycle.agent.md` → `.bak`; `orchestrator.agent.md` already has `.bak`.

**Step 3.** Update catalog.json, CHANGELOG.md → v7.0.0.

---

### Phase B: Context Window Optimization *(Steps 4–6)* *(parallel with Phase A)*

**Step 4.** Introduce **Phase Handoff Contracts** — each phase writes `artifacts/phase-{N}-summary.json`:
```
{ phaseId, completedAt, artifacts[], keyDecisions[], contextForNextPhase{}, assumptions[] }
```
- Max 500 tokens per summary — orchestrator passes `contextForNextPhase`, NOT the full YAML

**Step 5.** Create `.github/prompts/phase-handoff-contract.prompt.md` — schema definition + generation instruction

**Step 6.** Update orchestrator prompts for Phases 2–10 to consume compact summary, reference artifacts by path only

---

### Phase C: Per-Phase Stage Gates *(Steps 7–9)* *(depends on A)*

**Step 7.** Create `.github/agents/stage-gate.agent.md`
- Tools: `[codebase, search, runCommand, problems]` — NO `agent` tool (prevents circular delegation)
- `user-invokable: false` (subagent only; hidden from picker)
- Returns structured: `{passed, failures[{check, message, severity}], criticalCount, warningCount}`

**Step 8.** Gate contracts per phase transition:

| Gate | Check | Command/Rule |
|------|-------|------|
| 1→2 | Manifest completeness | projectName, ≥3 userStories, nfrs, complexityScore present |
| 2→3 | Decomposition completeness | All boundedContexts have apiSurface + dataEntities |
| 3→4 | Architecture readiness | ≥1 ADR, techStack, dataStore confirmed |
| **4→5** | **OpenAPI validity** | `openapi-generator validate specs/apis/openapi.yaml` exits 0 |
| **5→6** | **Backend compiles** | `mvn compile` exits 0 (phase-local fix loop for 5a) |
| 6→7 | Tests exist | `find src/test -name '*.java' \| wc -l` ≥ 10 |
| 7→8 | All CRITICAL checks passed | validation-report.yaml has no CRITICAL failures |

**Step 9.** Create `.github/prompts/stage-gate-check.prompt.md` — parameterized gate check template

---

### Phase D: Enhanced Self-Healing Loop *(Steps 10–13)* *(depends on A)*

**Step 10.** Add **root cause routing table** to pipeline-orchestrator:

| Failure Type | → Responsible Agent | Re-invoke With |
|---|---|---|
| Compilation error | `@backend-java` / `@spring-boot` | Error message + file context |
| Test failure | `@testing-qa` | Failing test + stack trace |
| OpenAPI contract mismatch | `@api-designer` | Code-vs-spec diff |
| Security vulnerability | `@security-compliance` | CVE report |
| DB migration error | `@database-engineer` | Flyway error + schema diff |
| Frontend build failure | `@frontend-react` | Build error + tsconfig |
| Requirements gap | `@requirement-analyst` | Unimplemented story IDs |

**Step 11.** **Partial re-validation** — persist failed checks in `reports/validation-failures.json`; on auto-fix cycle, re-run only the failed checks (not full Phase 7).

**Step 12.** **Phase-local fix loops** at stage gates:
- Phase 5a compile failure → re-invoke `@backend-java` (max 2 local cycles) BEFORE Phase 6 starts
- This quarantines compile errors at their source phase

**Step 13.** Update validation-to-autofix.handoff.md with root cause routing table.

---

### Phase E: Runtime Verification *(Steps 14–15)* *(depends on A)*

**Step 14.** Add **Phase 7b — Runtime Verification** inside `@validation-qa`:
1. Start app: `mvn spring-boot:run -Dspring-boot.run.fork=true &` (or `docker-compose up -d` if Dockerfile present)
2. Poll `GET /actuator/health` — 5 retries × 10s → expects `{"status":"UP"}`
3. Execute smoke tests (5–10 curl commands per bounded context, happy-path)
4. `kill %1` / `docker-compose down`, report results
5. FAIL → auto-fix routes to `@reliability-resilience`

**Step 15.** Create `.github/prompts/smoke-test-generation.prompt.md` — generates curl smoke tests from OpenAPI spec (1 per bounded context, validates HTTP status codes)

---

### Phase F: Observability & Hardening *(Steps 16–20)* *(parallel with E)*

**Step 16.** Per-phase structured observability:
- `traceId = uuid()` at phase start, written to `logs/phase-{N}-{traceId}.log`
- Fields: `{traceId, phase, agentId, startISO, endISO, durationMs, status, filesWritten[], tokensEstimate}`
- PIPELINE-EXECUTION.md gains `traceId` and `durationMs` columns

**Step 17.** Budget guards in pipeline-orchestrator:
- `MAX_DELEGATIONS_PER_PHASE = 3` — circuit breaker per phase
- `MAX_TOTAL_DELEGATIONS = 60` — total pipeline circuit breaker
- Sub-agent response > 8000 tokens → force summarization before next delegation
- On exhaustion: complete current phase, halt, write `reports/budget-exceeded.md`

**Step 18.** `reports/pipeline-trace.json` as Phase 10 artifact — aggregates all phase logs, decisions, auto-fix cycles, full traceability matrix (`userStoryId → file → test`).

**Step 19.** Optional **Phase 3.5 — Low-Level Design** (conditional on `manifest.complexity = HIGH|CRITICAL`):
- Agent: `@architect` with LLD-specific prompt
- Artifacts: `specs/lld/` (component diagrams, sequence diagrams, class structure)
- Gate: all bounded-context components have an LLD entry before Phase 4

**Step 20.** Agent scoping for new files only:
- `stage-gate.agent.md`: `user-invokable: false`, no `agent` tool
- `pipeline-orchestrator.agent.md`: explicit `agents: [...]` list, no wildcard

---

### Dependency Map

```
Phase A (Consolidation) ──────────────────────────────┐
Phase B (Context Optimization) [parallel with A]        │
                                                        ▼
Phase C (Stage Gates) ──────── depends on A            │
Phase D (Self-Healing) ──────── depends on A           │
Phase E (Runtime Verification) ── depends on A         │
Phase F (Observability) ──────── depends on A, parallel with E
```

---

### Files

**Create (7):**
- `.github/agents/pipeline-orchestrator.agent.md` — primary deliverable
- `.github/agents/stage-gate.agent.md` — phase boundary validator
- `.github/prompts/phase-handoff-contract.prompt.md`
- `.github/prompts/stage-gate-check.prompt.md`
- `.github/prompts/smoke-test-generation.prompt.md`
- `.github/skills/agentic-ai/pipeline-orchestration.md`
- `.github/handoffs/stage-gate-failure.handoff.md`

**Modify (4):**
- validation-qa.agent.md — Phase 7b + partial re-validation + root cause output
- validation-to-autofix.handoff.md — root cause routing table
- catalog.json — register new agents
- CHANGELOG.md — v7.0.0

**Archive (rename to .bak):**
- full-lifecycle.agent.md
- orchestrator.agent.md

---

### Verification

1. VS Code agent picker shows single "Pipeline Orchestrator" entry — not full-lifecycle or orchestrator separately
2. Run end-to-end with minimal CRUD PRD → `PIPELINE-EXECUTION.md` + source files exist in `PROJECT_ROOT`
3. Break Phase 1 manifest (remove all user stories) → stage-gate blocks Phase 2 with `criticalCount > 0`
4. Introduce compile error in Phase 5a → phase-local fix loop fires (not Phase 7)
5. Verify `artifacts/phase-1-summary.json` exists and is ≤ 500 tokens
6. Verify `logs/phase-1-{traceId}.log` and `reports/pipeline-trace.json` in Phase 10 output
7. Simple CRUD PRD → Phase 7b smoke test sees `{"status":"UP"}` from `/actuator/health`

---

### Decisions
- **Merge decision:** MERGE — full-lifecycle is a thin wrapper with no domain logic; Phase 0 of pipeline-orchestrator absorbs all workspace analysis cleanly
- **Phase 7b runtime verification:** CONDITIONAL on Actuator or Docker being present
- **Phase 3.5 LLD:** OPTIONAL, manifest-complexity-gated
- **Scoping changes:** Applied only to new files—existing 53 specialist agents are out of scope
- **Archive policy:** Keep .bak files 30 days before cleanup

### Excluded
- Refactoring existing 53 specialist agents' internals
- Changing catalog.json domain taxonomy
- Multi-repo pipeline support
- Changing the default tech stack

---

**Further Considerations**

1. **Parallelism clarity** — The plan documents Phase 5 as *sequential with parallel intent* (honest about VS Code's sequential `agent()` calls). If you want true parallel execution, a future enhancement would use a GitHub Actions matrix-style external trigger. Include this as a documented assumption in pipeline-orchestrator?

2. **Traceability matrix timing** — Should `traceability-matrix.json` be seeded at Phase 1 (with story IDs only) and enriched at each phase, or assembled entirely in Phase 10? Incremental is more resilient but adds complexity to each phase prompt.

3. **stage-gate agent vs. inline checks** — Stage gates could be implemented as inline validation commands in the orchestrator instead of a separate agent (lower overhead, fewer sub-agent invocations). Separate agent gives better failure isolation; inline is simpler. Preference?

---

Ready to implement on your approval. The biggest single-file change is `pipeline-orchestrator.agent.md` — the merge of the two existing files with all improvements applied. Shall I proceed?

















## Plan: Enterprise Agent-Driven SDLC — Full Cleanup + Pipeline v7.0

**TL;DR:** Merge `full-lifecycle` + `orchestrator` into a single `pipeline-orchestrator`, redesign with enterprise-grade stage gates, context handoff contracts, self-healing loop with root-cause routing, runtime verification, and structured observability. Simultaneously clean 59 files/dirs from .github (deprecated, duplicate, empty, OS artifacts, Spring-AI-specific, examples) — leaving a clean, executable system.

---

### Current State Assessment

| Area | Status | Issue |
|------|--------|-------|
| Orchestration | ❌ Fragmented | `full-lifecycle` + `orchestrator` = ~800 lines duplicated across 2 files |
| Agent count | ❌ Bloated | 19 agents are duplicates or subsumed by others |
| Handoffs | ❌ Stale | 5 explicitly `deprecated: true`, 2 more vague/unused |
| Prompts | ❌ Off-topic | 12 of 25 are Spring AI framework-specific (not pipeline prompts) |
| Architecture docs | ❌ Outdated | `automated-pipeline.md` describes a 13-phase v1 pipeline (now 10-phase v7) |
| Examples dir | ⚠️ Noise | 16 example files serve as docs, not executable artifacts |
| Stage gates | ❌ Missing | Zero per-phase validation — single gate at Phase 7 only |
| Self-healing | ⚠️ Narrow | Auto-fix only from Phase 7; compile errors at Phase 5a reach Phase 6 unchanged |
| Runtime verification | ❌ Missing | No app startup + smoke test + health check |
| Context handling | ❌ Bloated | Raw YAML blobs passed between phases — no compact handoff contracts |
| Observability | ❌ Missing | No trace IDs, no per-phase structured logs |
| Budget guards | ❌ Missing | No delegation limit; runaway cascade possible |

---

### Phase 1: File Cleanup (Delete — no logic changes) *(independent, do first)*

#### 1a — Agent Deletions (19 files)

| File | Reason |
|------|--------|
| `agents/full-lifecycle.agent.md` | Merged into `pipeline-orchestrator` |
| `agents/orchestrator.agent.md` | Merged into `pipeline-orchestrator` |
| `agents/orchestrator.agent.md.bak` | Stale backup |
| `agents/database.agent.md` | Full duplicate of `database-engineer.agent.md` |
| `agents/devops-cicd.agent.md` | Full duplicate of `devops-engineer.agent.md` |
| `agents/documentation-generator.agent.md` | Full duplicate of `documentation-writer.agent.md` |
| `agents/refactoring.agent.md` | Full duplicate of `refactoring-workflow.agent.md` |
| `agents/regular-dev-orchestrator.agent.md` | Replaced by `pipeline-orchestrator` |
| `agents/spec-driven-orchestrator.agent.md` | Spec-driven behaviour embedded in pipeline-orchestrator Phase 4 |
| `agents/agentic-orchestration.agent.md` | Meta-level design agent; orchestration logic lives in pipeline-orchestrator |
| `agents/bdd-orchestrator.agent.md` | BDD delegated to `testing-qa` + `skills/bdd/` |
| `agents/tdd-orchestrator.agent.md` | TDD delegated to `testing-qa` + `skills/tdd/` |
| `agents/standards-enforcement.agent.md` | Standards checks owned by `validation-qa` |
| `agents/code-quality.agent.md` | Code quality owned by `validation-qa` |
| `agents/contract-testing.agent.md` | Contract tests owned by `testing-qa` + `validation-qa` |
| `agents/data-modeling.agent.md` | Fully covered by `database-engineer.agent.md` |
| `agents/compliance.agent.md` | GDPR/SOC2/PCI delegated to `security-compliance.agent.md` |
| `agents/llm-platform.agent.md` | LLM platform logic owned by `ai-ml-engineer.agent.md` |
| `agents/ai-observability.agent.md` | AI observability owned by `observability.agent.md` |

**Agents after cleanup: 38 (36 kept + 2 new below)**

#### 1b — Handoff Deletions (7 files — all `deprecated: true` or vague)

| File | Reason |
|------|--------|
| `handoffs/dev-to-security.handoff.md` | `deprecated: true` — security in Phase 7 |
| `handoffs/documentation-to-validation.handoff.md` | `deprecated: true` — wrong phase order |
| `handoffs/openapi-to-contract.handoff.md` | `deprecated: true` — contract tests in Phase 6 |
| `handoffs/security-to-devops.handoff.md` | `deprecated: true` — security in Phase 7 |
| `handoffs/testing-to-security.handoff.md` | `deprecated: true` — security in Phase 7 |
| `handoffs/feature-to-ai-ml.handoff.md` | Not a pipeline step; no clear trigger or output contract |
| `handoffs/feature-to-resilience.handoff.md` | Not a pipeline step; no clear trigger or output contract |

#### 1c — Prompt Deletions (12 files — Spring AI specific / non-pipeline)

| File | Reason |
|------|--------|
| `prompts/agent-execution.prompt.md` | Spring AI framework internals, not SDLC pipeline |
| `prompts/agent-planning.prompt.md` | Spring AI framework internals |
| `prompts/agent-reflection.prompt.md` | Spring AI framework internals |
| `prompts/agent-termination.prompt.md` | Spring AI framework internals |
| `prompts/agentic-flow.prompt.md` | Agent design meta-prompt; not used by any pipeline phase |
| `prompts/ai-tradeoff-analysis.prompt.md` | AI architecture meta; not a pipeline artifact |
| `prompts/llm-system-design.prompt.md` | LLM system design; handled by `ai-ml-engineer` agent directly |
| `prompts/rag-design.prompt.md` | RAG design; handled by `rag.agent.md` |
| `prompts/spring-ai-rag.prompt.md` | Spring AI specific |
| `prompts/spring-ai-system-design.prompt.md` | Spring AI specific |
| `prompts/spring-ai-tooling.prompt.md` | Spring AI specific |
| `prompts/spring-ai-tradeoffs.prompt.md` | Spring AI specific |

#### 1d — Architecture folder deletions (3 files — stale/framework-specific)

| File | Reason |
|------|--------|
| `architecture/agent-lifecycle.md` | Spring AI–specific lifecycle doc; duplicated in `skills/agentic-spring-ai/` |
| `architecture/failure-and-recovery.md` | Spring AI–specific; duplicated in `skills/agentic-ai/failure-modes.md` |
| `architecture/spring-ai-agentic-architecture.md` | Spring AI–specific framework doc; covered by `skills/agentic-spring-ai/` |

**UPDATE instead of delete:** `architecture/automated-pipeline.md` — refresh to v7.0 10-phase architecture.

#### 1e — Other deletions

| Path | Reason |
|------|--------|
| `examples/` (all 16 files + dir) | Pure reference material; not executable; teams can regenerate |
| `appmod/appcat/` | Completely empty directory |
| .DS_Store | macOS metadata artifact |
| `skills/.DS_Store` | macOS metadata artifact |

---

### Phase 2: Create New Files *(core deliverables)*

**Step 1.** **`.github/agents/pipeline-orchestrator.agent.md`** — the primary deliverable

Architecture:
```
FRONTMATTER:
  name: Pipeline Orchestrator
  tools: [codebase, editFiles, fetch, search, runCommand, agent]
  agents: [requirement-analyst, product-decomposition, architect, api-designer, openapi,
           asyncapi, backend-java, spring-boot, frontend-react, database-engineer,
           ai-ml-engineer, kafka-streaming, testing-qa, validation-qa, documentation-writer,
           devops-engineer, security-compliance, reliability-resilience, self-learning,
           stage-gate]  ← EXPLICIT list, no '*' wildcard

PHASE 0: Workspace Analysis + Project Setup (absorbed from full-lifecycle)
  - Workspace classification: AGENTS_META | EXISTING_PROJECT | EMPTY_WORKSPACE
  - TARGET_DIR derivation, context file registry, CONTEXT_FILES_REGISTRY (immutable)
  - mkdir -p PROJECT_ROOT verification
  - git init, PIPELINE-EXECUTION.md initialization
  - Resume check: scan for existing PIPELINE-EXECUTION.md

PHASES 1–10: (existing 10-phase pipeline, with these additions)
  - After each phase: write artifacts/phase-{N}-summary.json (compact handoff contract)
  - Before each phase transition: invoke stage-gate agent
  - Phase 5a local fix loop (max 2 cycles before continuing)
  - Phase 7b Runtime Verification (embedded in validation-qa call)
  - Per-phase trace ID + log file

BUDGET_GUARDS:
  - MAX_DELEGATIONS_PER_PHASE = 3
  - MAX_TOTAL_DELEGATIONS = 60
  - Response > 8000 tokens → force summarization

ROOT_CAUSE_ROUTING TABLE (self-healing):
  compile_error → @backend-java / @spring-boot
  test_failure → @testing-qa
  contract_mismatch → @api-designer
  security_vulnerability → @security-compliance
  db_migration_error → @database-engineer
  frontend_build_failure → @frontend-react
  requirements_gap → @requirement-analyst

PIPELINE-EXECUTION.md dashboard: gains traceId + durationMs columns

ANTI-FAKE-COMPLETION RULES: 5 critical rules enforced with explicit runCommand verification
```

**Step 2.** **`.github/agents/stage-gate.agent.md`**
```
FRONTMATTER:
  name: Stage Gate
  user-invokable: false
  tools: [codebase, search, runCommand, problems]  ← NO agent tool (prevents circular)

RESPONSIBILITIES:
  - Called by pipeline-orchestrator at each phase boundary
  - Runs exit-criteria checks per phase (manifests, artifacts, compilation, spec validity)
  - Returns JSON: { passed, failures[{checkId, message, severity}], criticalCount }
  - criticalCount > 0 → orchestrator triggers phase-local fix loop, NOT auto-advance

GATE CONTRACTS:
  Phase 1→2: manifest has projectName + ≥3 userStories + nfrs + complexityScore
  Phase 2→3: all boundedContexts have apiSurface + dataEntities + buildOrder
  Phase 3→4: ≥1 ADR + techStack + dataStore confirmed in architecture/
  Phase 4→5: openapi-generator validate exits 0 on specs/apis/openapi.yaml
  Phase 5→6: mvn compile exits 0 AND src/main/java has ≥1 .java file
  Phase 6→7: find src/test -name '*.java' | wc -l ≥ 10
  Phase 7→8: reports/validation-report.yaml has 0 CRITICAL failures
```

**Step 3.** **`.github/prompts/phase-handoff-contract.prompt.md`**
```
PhaseHandoffContract JSON schema:
{
  phaseId: int,
  completedAt: ISO8601,
  durationMs: int,
  traceId: uuid,
  artifacts: string[],        ← paths only, no content
  keyDecisions: string[],     ← max 5 bullets
  contextForNextPhase: {      ← ONLY what next agent needs, ≤500 tokens
    projectName: string,
    techStack: object,
    boundedContexts: string[],
    criticalConstraints: string[],
    [phaseSpecificFields]: ...
  },
  assumptions: string[]
}
```

**Step 4.** **`.github/prompts/stage-gate-check.prompt.md`**
- Parameterized: `{fromPhase, toPhase, exitCriteria[], projectRoot}`
- Returns structured gate result JSON

**Step 5.** **`.github/prompts/smoke-test-generation.prompt.md`**
- Input: OpenAPI spec path, bounded contexts list
- Output: 5–10 curl commands, one per bounded context, happy-path only, validates HTTP status codes

**Step 6.** **`.github/skills/agentic-ai/pipeline-orchestration.md`**
- New skill: SDLC pipeline orchestration patterns — stage gate design, phase handoff contracts, root-cause routing, budget guards, partial re-validation

**Step 7.** **`.github/handoffs/stage-gate-failure.handoff.md`**
- When stage gate returns `criticalCount > 0` — routes to appropriate fix agent with structured failure context

---

### Phase 3: Update Existing Files *(depends on Phase 1 cleanup)*

**Step 8.** **`agents/validation-qa.agent.md`**
- Add Phase 7b — Runtime Verification section:
  1. `mvn spring-boot:run` (background) or `docker-compose up -d` if Dockerfile
  2. Poll `/actuator/health` with timeout (5×10s)
  3. Run smoke tests from `reports/smoke-tests.sh`
  4. Report `{runtimeVerified, healthCheckStatus, smokeTestResults}`
- Add `reports/validation-failures.json` output for partial re-validation tracking
- Add `rootCause` field to auto-fix-report output schema

**Step 9.** **`handoffs/validation-to-autofix.handoff.md`**
- Add root-cause routing table (compile/test/contract/security/db/frontend/requirements → agent mapping)

**Step 10.** **`architecture/automated-pipeline.md`**
- Update from v1 13-phase to v7.0 10-phase architecture
- Add Phase 0 workspace analysis, Phase 7b runtime verification, stage gate diagram
- Add handoff contract pattern, budget guard table

**Step 11.** **`catalog.json`**
- Remove 19 deleted agent entries
- Add `pipeline-orchestrator`, `stage-gate` entries
- Update version to `7.0.0`, `lastUpdated: 2026-04-13`

**Step 12.** **`CHANGELOG.md`**
- Add v7.0.0 entry with full change summary

**Step 13.** **copilot-instructions.md** (the meta workspace instructions)
- Update agent references: replace full-lifecycle/orchestrator → pipeline-orchestrator
- Remove references to deleted agents
- Update agent count (38 → not 53)

**Step 14.** **`skills-index.md`**
- Add entry for new `pipeline-orchestration.md` skill

---

### Phase Dependency Order

```
Phase 1 (Cleanup) ──────────────────────────────────── can start immediately
Phase 2 (New Files) ───────────────── independent of Phase 1, can run in parallel  
Phase 3 (Updates) ──────────────────── depends on Phase 1 + Phase 2 being complete
```

---

### Files Summary

**DELETE (58 items):**
- 19 agent files
- 7 handoff files
- 12 prompt files
- 3 architecture files
- 16 example files + `examples/` dir
- `appmod/appcat/` dir
- 2 .DS_Store files

**CREATE (7 new files):**
- `agents/pipeline-orchestrator.agent.md`
- `agents/stage-gate.agent.md`
- `prompts/phase-handoff-contract.prompt.md`
- `prompts/stage-gate-check.prompt.md`
- `prompts/smoke-test-generation.prompt.md`
- `skills/agentic-ai/pipeline-orchestration.md`
- `handoffs/stage-gate-failure.handoff.md`

**UPDATE (7 existing files):**
- `agents/validation-qa.agent.md`
- `handoffs/validation-to-autofix.handoff.md`
- `architecture/automated-pipeline.md`
- `catalog.json`
- `CHANGELOG.md`
- copilot-instructions.md
- `skills-index.md`

**Final state: 38 agents, 13 prompts, 24 handoffs, 27 skill domains, 2 architecture docs**

---

### Verification

1. VS Code agent picker shows "Pipeline Orchestrator" — `full-lifecycle` and `orchestrator` absent
2. `agents/` directory contains exactly 38 `.agent.md` files (no `.bak`, no duplicates)
3. `prompts/` contains exactly 16 files (13 kept + 3 new)
4. `handoffs/` contains exactly 24 files (no `deprecated: true` entries)
5. `examples/` directory does not exist
6. All `catalog.json` entries have matching files on disk (no orphaned references)
7. Run pipeline with minimal CRUD PRD → stage gate at Phase 4→5 fails if OpenAPI spec invalid
8. Run pipeline → `artifacts/phase-1-summary.json` ≤ 500 tokens
9. Run pipeline → `/actuator/health` returns `{"status":"UP"}` in Phase 7b output
10. Break Phase 5a compilation → local fix loop fires before Phase 6

---

### Decisions

- **Merge**: `full-lifecycle` + `orchestrator` → `pipeline-orchestrator` (single entry point, zero duplication)
- **Stage Gate**: Separate `stage-gate.agent.md` with no `agent` tool (prevents circular delegation; enterprise isolation)
- **Context handoff**: Compact JSON summary (`artifacts/phase-N-summary.json`) replaces raw YAML blob passing — solves context window bloat
- **Examples removed**: Not executable at runtime; teams generate them on demand via `learning.agent.md`
- **Skills kept in full**: All 27 skill domains are domain knowledge referenced by pipeline agents at execution time
- **Spring AI architecture docs removed**: Duplicated by `skills/agentic-spring-ai/` content; creates confusion between "Spring AI framework" and "SDLC pipeline architecture"
- **Explicit `agents:` list** on pipeline-orchestrator (not `'*'`) — prevents accidental delegation to unrelated agents (e.g., caching-strategy, legacy-modernization) mid-pipeline
- **Phase 7b runtime verification**: CONDITIONAL on Spring Boot Actuator or Docker presence (not a hard requirement)

### Excluded from this plan

- Rewriting the body/instructions of existing 36 specialist agents
- Changing skill domain content
- Multi-repo pipeline support
- Infrastructure provisioning via Terraform (remains in devops-engineer scope)

---

**Further Considerations (need answers before implementation starts)**

**1. Skills directory — should it be cleaned further?**
The `skills/` folder has `AUDIT_REPORT.md` and `VALIDATION_CHECKLIST.md` at the root (not inside a subdomain folder). Keep as pipeline quality artifacts, or remove?
- **Option A**: Keep (useful governance docs) — *recommended*
- **Option B**: Remove (clutter at root level)

**2. `architecture/` folder — keep or absorb?**
After removing 3 Spring AI–specific files, only `multi-agent-coordination.md` + updated `automated-pipeline.md` remain.
- **Option A**: Keep `architecture/` folder with 2 files — *recommended*
- **Option B**: Move files to root .github and delete folder

**3. copilot-instructions.md vs instructions.md**
Two instruction files exist at .github root (copilot-instructions.md and `instructions.md`). `instructions.md` appears to be a verbose version of copilot-instructions.md. Archive `instructions.md` or merge?
- **Option A**: Delete `instructions.md`, keep copilot-instructions.md as single source of truth — *recommended*
- **Option B**: Keep both (different audiences)

---

Ready to implement once you confirm these three points. Shall I proceed with Option A for all three (the recommended choices) and start implementation?